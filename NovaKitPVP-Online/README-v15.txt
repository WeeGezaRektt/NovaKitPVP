NovaKitPVP V15

Changes:
- Tier editor is a two-column card layout instead of one long stack.
- Each kit clearly labels Current / Peak / Retired.
- Audit logs become human-readable summaries with raw JSON hidden under Technical details.
- Staff creation now includes a Staff Username field.
- Staff list shows username prominently and email underneath.
- Future audit entries use staff usernames where available.
- Reset All Tiers / Factory Reset backend was fixed to satisfy safe-update rules.

The Supabase reset functions and audit username changes were already applied to the live database.
